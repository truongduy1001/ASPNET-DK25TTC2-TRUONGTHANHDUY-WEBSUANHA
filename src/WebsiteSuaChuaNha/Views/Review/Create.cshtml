@model WebsiteDichVuSuaNha.ViewModels.CreateReviewViewModel

@{
    ViewData["Title"] = "Đánh Giá Dịch Vụ";
    var booking = ViewBag.Booking as WebsiteDichVuSuaNha.Models.Booking;
}

<div class="row justify-content-center mt-4">
    <div class="col-md-8">
        <div class="card fade-in">
            <div class="card-header">
                <h4 class="mb-0">⭐ Đánh Giá Dịch Vụ</h4>
            </div>
            <div class="card-body">
                @if (booking != null)
                {
                    <div class="alert alert-info mb-4">
                        <h6>Thông Tin Đơn Hàng #@booking.Id</h6>
                        <p class="mb-1"><strong>Ngày sửa chữa:</strong> @booking.ServiceDate.ToString("dd/MM/yyyy")</p>
                        <p class="mb-1"><strong>Địa chỉ:</strong> @booking.Address</p>
                        <p class="mb-0"><strong>Dịch vụ:</strong> @booking.ServiceType</p>
                    </div>
                }

                <form asp-action="Create" method="post">
                    <input type="hidden" asp-for="BookingId" />
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <div class="mb-4">
                        <label asp-for="Rating" class="form-label fw-bold"></label>
                        <div class="star-rating">
                            <input type="radio" asp-for="Rating" value="5" id="star5" />
                            <label for="star5" title="5 sao">★</label>
                            <input type="radio" asp-for="Rating" value="4" id="star4" />
                            <label for="star4" title="4 sao">★</label>
                            <input type="radio" asp-for="Rating" value="3" id="star3" />
                            <label for="star3" title="3 sao">★</label>
                            <input type="radio" asp-for="Rating" value="2" id="star2" />
                            <label for="star2" title="2 sao">★</label>
                            <input type="radio" asp-for="Rating" value="1" id="star1" />
                            <label for="star1" title="1 sao">★</label>
                        </div>
                        <span asp-validation-for="Rating" class="text-danger"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Comment" class="form-label fw-bold"></label>
                        <textarea asp-for="Comment" class="form-control" rows="5" placeholder="Chia sẻ trải nghiệm của bạn về dịch vụ chuyển nhà..."></textarea>
                        <span asp-validation-for="Comment" class="text-danger"></span>
                        <small class="text-muted">Tối thiểu 10 ký tự, tối đa 1000 ký tự</small>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">📝 Gửi Đánh Giá</button>
                        <a asp-controller="Booking" asp-action="History" class="btn btn-outline-secondary">← Quay Lại</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

<style>
    .star-rating {
        direction: rtl;
        display: inline-flex;
        font-size: 3rem;
        gap: 0.5rem;
    }

    .star-rating input[type="radio"] {
        display: none;
    }

    .star-rating label {
        color: #ddd;
        cursor: pointer;
        transition: color 0.2s;
    }

    .star-rating input[type="radio"]:checked ~ label,
    .star-rating label:hover,
    .star-rating label:hover ~ label {
        color: #ffc107;
    }
</style>
