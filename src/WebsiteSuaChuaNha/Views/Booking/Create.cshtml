@model WebsiteDichVuSuaNha.Models.Booking

@{
    ViewData["Title"] = "Đặt Lịch Sửa Chữa";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card p-4 fade-in">
            <div class="text-center mb-4">
                <h2 class="text-primary">🛠️ Đặt Lịch Sửa Chữa</h2>
                <p class="text-muted">Điền thông tin bên dưới để chúng tôi hỗ trợ bạn nhanh nhất</p>
            </div>

            <form asp-action="Create" method="post" id="bookingForm">
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="CustomerName" class="form-label"></label>
                        <input asp-for="CustomerName" class="form-control" placeholder="Nguyễn Văn A" required />
                        <span asp-validation-for="CustomerName" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="PhoneNumber" class="form-label"></label>
                        <input asp-for="PhoneNumber" class="form-control" placeholder="0909xxxxxx" required />
                        <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="ServiceDate" class="form-label"></label>
                    <input asp-for="ServiceDate" class="form-control" type="date" required />
                    <span asp-validation-for="ServiceDate" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Address" class="form-label"></label>
                    <input asp-for="Address" class="form-control" placeholder="Số nhà, đường, phường, quận..." required />
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ServiceType" class="form-label"></label>
                    <select asp-for="ServiceType" class="form-select" required>
                        <option value="">-- Chọn loại dịch vụ --</option>
                        <option value="Sửa chữa điện">⚡ Sửa chữa điện</option>
                        <option value="Sửa chữa nước">💧 Sửa chữa nước</option>
                        <option value="Sơn nhà & Chống thấm">🎨 Sơn nhà & Chống thấm</option>
                        <option value="Sửa chữa tổng hợp">🛠️ Sửa chữa tổng hợp</option>
                        <option value="Khác">❓ Khác</option>
                    </select>
                    <span asp-validation-for="ServiceType" class="text-danger"></span>
                </div>

                <div class="alert alert-info" id="costEstimate">
                    <h5>💵 Phí Dịch Vụ Cơ Bản:</h5>
                    <p class="mb-0"><strong id="estimatedCost">@ViewBag.BaseServiceFee.ToString("N0") VNĐ</strong></p>
                    <small class="text-muted">
                        Đây là phí khảo sát và sửa chữa nhỏ. Chi phí thực tế sẽ được báo sau khi thợ kiểm tra.
                    </small>
                </div>

                <input asp-for="EstimatedCost" type="hidden" value="@ViewBag.BaseServiceFee" />

                <div class="mb-3">
                    <label asp-for="Notes" class="form-label"></label>
                    <textarea asp-for="Notes" class="form-control" rows="3" placeholder="Mô tả chi tiết sự cố (ví dụ: bóng đèn không sáng, vòi nước rò rỉ...)"></textarea>
                </div>

                <div class="d-grid gap-2 mt-4">
                    <button type="submit" class="btn btn-primary btn-lg">✅ Xác Nhận Đặt Lịch</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
