@{
    ViewData["Title"] = "Tra Cứu Đơn Hàng";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card p-4 fade-in">
            <div class="text-center mb-4">
                <h2 class="text-primary">🔍 Tra Cứu Đơn Hàng</h2>
                <p class="text-muted">Nhập số điện thoại để xem lịch sử đặt lịch của bạn</p>
            </div>

            <form asp-action="TrackResult" method="post">
                <div class="mb-3">
                    <label for="phoneNumber" class="form-label fw-bold">Số Điện Thoại</label>
                    <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" placeholder="0909xxxxxx" required />
                </div>

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary btn-lg">Tra Cứu</button>
                </div>
            </form>
        </div>
    </div>
</div>
